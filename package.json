{
  "name": "ccpv",
  "version": "0.1.0",
  "private": true,
  "homepage": "https://ccpv.app",
  "dependencies": {
    "react": "^16.3.2",
    "react-dom": "^16.3.2",
    "react-scripts": "1.1.4"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject",
    "deploy": "gh-pages -d build -m \"Updates\nCircleCI [skip ci]\"",
    "prettier": "prettier src/**/*.js --write --no-semi false --single-quote true"
  },
  "devDependencies": {
    "gh-pages": "^1.1.0",
    "prettier": "^1.12.1"
  }
}
